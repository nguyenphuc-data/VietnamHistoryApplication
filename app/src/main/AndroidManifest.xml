<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VietnamHistoryApplication">
        <activity
            android:name=".game.quizzes.QuizzesDetail"
            android:exported="false" />
        <!-- Activity chính (launcher) -->
        <activity
            android:name=".main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Các Activity khác -->
        <activity
            android:name=".home.HomeActivity"
            android:exported="false" />

        <activity
            android:name=".stage.StageActivity"
            android:exported="false"
            android:label="Stage Activity" />

        <!-- Thêm khai báo cho StageDetailActivity -->
        <activity
            android:name=".stageDetail.StageDetailActivity"
            android:exported="false"
            android:label="Chi tiết Giai đoạn" />

        <!-- Thêm khai báo cho PersonListActivity -->
        <activity
            android:name=".person.PersonList.PersonListActivity"
            android:exported="false"
            android:theme="@style/Theme.VietnamHistoryApplication" />

        <activity
            android:name=".person.PersonDetail.PersonDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.VietnamHistoryApplication" />

    </application>

</manifest>