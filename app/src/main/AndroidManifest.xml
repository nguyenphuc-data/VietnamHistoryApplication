<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VietnamHistoryApplication">

        <activity
            android:name=".stage.StageActivity"
            android:exported="false"
            android:label="Stage Activity" />

        <activity
            android:name=".period.PeriodDetailActivity"
            android:exported="false" />

        <activity
            android:name=".home.ProfileFragment.ProfileFragment"
            android:exported="false" />

        <activity
            android:name=".home.ExploreFragment.ExploreFragment"
            android:exported="false" />

        <activity
            android:name=".home.GameFragment.GameFragment"
            android:exported="false" />

        <activity
            android:name=".home.CharacterFragment.CharacterFragment"
            android:exported="false" />

        <activity
            android:name=".home.PeriodFragment.PeriodFragment"
            android:exported="false" />

        <activity
            android:name=".home.HomeActivity"
            android:exported="false" />

        <activity
            android:name=".main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Thêm khai báo cho StageDetailActivity -->
        <activity
            android:name=".stage.StageDetailActivity"
            android:exported="false"
            android:label="Chi tiết Giai đoạn" />

    </application>

</manifest>